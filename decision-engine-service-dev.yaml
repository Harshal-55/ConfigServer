logging:
  level:
    root: INFO
    com.cts.gemini_test_try2: DEBUG

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}
    fetch-registry: true
    register-with-eureka: true

  instance:
    prefer-ip-address: true

google:
  api:
    key: ${GEMINI_API_KEY}
